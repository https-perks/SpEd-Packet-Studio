{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default", "description": "Default capability for the main application window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-set-title"]
}
